// - Un nuevo Set con los nombres de tu familia
const names = new Set(['Marivel', 'Jose Angel', 'Abel', 'Efrain']);
console.log(names);

// - Modifica el Set original añadiendo tu nombre (duplicado) (debería darte lo mismo)
names.add('Efrain');
console.log(names);

// - Modifica el Set original añadiendo tu nombre (duplicado) (debería darte lo mismo)
names.add('Javascript');
console.log(names);